# Configuration file for dot (https://github.com/ksonj/dot.git)

# Where to find the dotfile repository
DOTHOST=USER@HOST
DOTREMOTEDIR=/home/USER/repos/dotfiles.git
DOTPROTOCOL=ssh  # only ssh is supported

DOTREMOTE=${DOTPROTOCOL}://${DOTHOST}:${DOTREMOTEDIR}

# Where to put the git repository (instead of just .git)
DOTGIT=${HOME}/dotfiles.git
